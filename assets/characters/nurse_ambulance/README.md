# Nurse Ambulance Character Slot

The previous static nurse mesh has been removed from active project content.

The active first-pass rigged nurse character is:

`nurse_actor_rigged.glb`

The matching inactive staging data is:

`data/characters/nurse_ambulance.performance.json`

Keep the actor id `nurse_actor`; room timelines and animation bindings use that stable id.
